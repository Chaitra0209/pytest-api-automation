[pytest]
testpaths = tests
addopts = -v --tb=short --html=reports/report.html --self-contained-html
markers =
    smoke: Quick sanity check tests - run on every build
    regression: Full regression suite
    api: API layer tests
